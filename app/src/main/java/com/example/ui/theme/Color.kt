package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Geometric Balance Theme Colors (Teal + Slate)
val FarmDarkGreen = Color(0xFF0F172A) // Slate-900 (Login gradient top / dark ambient environment background)
val FarmHeaderTeal = Color(0xFF0F766E) // Teal-700 (Primary headers, appbars)
val FarmMintGreen = Color(0xFF0D9488) // Teal-600 (Main buttons, dynamic selected state)
val FarmMediumTeal = Color(0xFF14B8A6) // Teal-500 (Accents and secondary states)
val FarmBlueAccent = Color(0xFF3B82F6) // Slate-compatible blue accent (Income items, informational details)
val FarmRedAccent = Color(0xFFEF4444) // Slate-compatible red accent (Alarm/Expense/Rejection details)
val FarmCardBackground = Color(0xFFF1F5F9) // Slate-100 (Secondary quiet background card / containers)

// Theme base palettes
val PrimaryGreen = Color(0xFF0F766E) // Teal-700
val SecondaryGreen = Color(0xFF0D9488) // Teal-600
val DarkBackground = Color(0xFF0F172A) // Slate-900
val LightBackground = Color(0xFFF3F6F6) // Geometric Balance HTML body background
val LightSurface = Color(0xFFFFFFFF) // Pure white
val DarkSurface = Color(0xFF1E293B) // Slate-800

